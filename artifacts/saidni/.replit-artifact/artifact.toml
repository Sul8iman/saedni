kind = "web"
previewPath = "/"
title = "ساعدني"
version = "1.0.0"
id = "artifacts/saidni"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24796

[services.development]
run = "pnpm --filter @workspace/saidni run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/saidni", "run", "build" ]
publicDir = "artifacts/saidni/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24796"
BASE_PATH = "/"
